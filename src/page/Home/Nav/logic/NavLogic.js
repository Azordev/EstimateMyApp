import React from 'react';
import NavView from '../visual/NavView';


const NavLogic = ({logo='Logo', navLinks='botonera'}) => (
    
    <NavView 
        logo={logo}
        navLinks={navLinks}
    />
    
)

export default NavLogic